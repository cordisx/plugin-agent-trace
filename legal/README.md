# Third-party naming allowlist

`public-name-policy.json` contains the exact external package identifiers that
must remain visible for module resolution, dependency integrity, attribution,
and license review. It is not a product vocabulary exception.

The naming gate rejects every non-allowlisted occurrence in repository and
generated package artifacts.
